# Runtime dependencies (what ships in the Docker image).
fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
jinja2>=3.1
tzdata>=2024.1        # zoneinfo data for the footer's build date (python:slim ships none)
